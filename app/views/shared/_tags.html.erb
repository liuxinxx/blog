<div class="card">
  <div class="card-header" align="center">标签</div>
  <div class="card-body">
    <% @tags.each do |t|%>
    <% if t.articles.size > 0%>
    <a class='badge badge-info' href="<%=tag_index_path(:tag_name => t.tag_name)%>"><%=t.tag_name%> <span><%=t.articles.size.to_s%></span></a>
    <%end%>
    <%end%>
  </div>
</div>